---
title: "Inviting Users via CSV Upload"
description: "Shelf allows you to invite multiple users simultaneously by uploading a CSV file containing their information, making it useful for onboarding teams or departments all at once."
category: "Other"
date: "2024-06-01"
updated: "2026-09-22"
layout: "knowledge-base"
---

## Overview

Shelf enables bulk user invitations through CSV file uploads, making it particularly useful for onboarding teams or departments all at once.


![Team settings page with Import Users button for CSV upload](https://qliecghuzfchfjwaisyx.supabase.co/storage/v1/object/public/website-images/knowledgebase/inviting-csv-1.webp)
## Prerequisites

- Administrator permissions required in your workspace
- CSV file formatted according to the provided template
- Team subscription (user management is a team feature)

## Getting Started

1. Navigate to **Settings > Team**
2. Click on **Import Users**
3. Download the CSV template to ensure proper formatting
4. Fill in the template with user information


## CSV File Requirements

### Base Rules

- Use comma (,) as the delimiter
- Valid roles: ADMIN, BASE, SELF_SERVICE
- Role column is case-sensitive
- Each row represents one new user
- Email addresses must be valid
- First row reserved for column headers

**There is no OWNER role in this file.** An import can create Administrators, Self-service and Base users, and nothing else. Ownership moves only through [Transferring Workspace Ownership](/knowledge-base/transfer-workspace-ownership), which demotes the outgoing owner, handles the subscription, and emails both people — none of which an invite can do.

### Required Columns

- **role**: User's workspace role (ADMIN, BASE, or SELF_SERVICE)
- **email**: User's email address

### Sample CSV Format

```
role,email
ADMIN,john.doe@company.com
SELF_SERVICE,jane.smith@company.com
```

## Import Process

1. Prepare CSV file according to template
2. Click **Import Users** in Team settings
3. Add optional welcome message for invitation emails
4. Select prepared CSV file
5. Click **Import now**


## After Import

- Users receive email invitations
- System validates all entries before processing
- Summary provided of successful invites and errors
- New users must accept invitation to gain access

### If a Role Is Wrong

Every row's role is checked before a single invitation goes out, and a bad value stops the whole file rather than being quietly skipped. Shelf names the rows to fix, using the row numbers you see in your spreadsheet:

> Roles must be one of ADMIN, BASE, SELF_SERVICE. Ownership cannot be granted by invite — use ownership transfer instead. Problem rows: row 4 ("OWNER"), row 9 ("admin").

Correct those cells and upload the file again. Nothing from a rejected file is sent, so there is no half-finished import to unpick and no need to work out which of your ten listed people actually got an email.

Note the second example: `admin` in lower case is rejected too. The column is case-sensitive, so the three accepted values have to be typed exactly as `ADMIN`, `BASE`, and `SELF_SERVICE`.

### Capital Letters in an Email Address Do Not Matter

An email address means the same person however it is typed, and Shelf now treats it that way. `First.Last@company.com` and `first.last@company.com` are one address, not two.

That settles three things an import used to get wrong:

- A row listing an address that is **already a member** in different capitals is skipped as already in the workspace, instead of inviting that person a second time.
- A file listing **one address twice** in different capitals creates **one** invite, not two.
- An address that already has a **pending invite** in different capitals is recognised as the same pending invite.

Addresses are stored in lower case from the import onward, and leading or trailing spaces are trimmed. The team member's **name** still comes from the address exactly as you typed it, because that becomes the first name of the account created when the person accepts.

Invites already sitting in your workspace with capital letters keep working as they are. Nothing about your existing data changed.

> **If someone told you the invite link said "Wrong user"**, this was why, and it is fixed. An invite created by a CSV import kept the capitals, while the person's account email was lower case, so opening the link while signed in refused them. Signed out, someone who already had a Shelf account got an error page instead of joining. Both now work. See [Troubleshooting: Team Invite Not Received](/knowledge-base/troubleshooting-invite-not-received).

## Important Notes

- All entries validated before processing
- Invites sent via email with secure join link
- Users already in workspace cannot be invited again, whatever the capitalisation of the address
- Email addresses must be correctly formatted

## Troubleshooting

- Verify CSV uses comma delimiter
- Check roles are correctly capitalized, and that none of them says `OWNER`
- Ensure all email addresses are valid
- Confirm users are not already workspace members

## Best Practices

- Review CSV before upload for accuracy
- Include clear welcome message with context or instructions
- Keep track of sent invitations
- Follow up with users who have not accepted

**Support Contact:** [hello@shelf.nu](mailto:hello@shelf.nu)
